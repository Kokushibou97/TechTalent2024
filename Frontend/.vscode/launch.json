{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Edge with local HTML file",
            "file": "${workspaceFolder}/index.html",
            "webRoot": "${workspaceFolder}",
            "browser": "edge"
        }
    ]
}

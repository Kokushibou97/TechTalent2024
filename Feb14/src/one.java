public class one {

	public static void main(String[] args) {
		int op1=3;
int op2=5;
sumaNumeros(op1, op2);
	}//here we execute//
public static int sumaNumeros(int num1, int num2) {
	int resultado= num1+num2;
	System.out.println("El resultado es "+resultado);
	return resultado;
}//here we do the action// the reason the method is outside because so it can be used as much as desired.//
}
